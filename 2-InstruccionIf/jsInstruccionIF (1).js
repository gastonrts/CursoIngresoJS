function mostrar()

	//2 POSIBLES VALORES 'TRUE' O 'FALSE' 
				// LAS ESTRUCTURAS IF NO LLEVAN PUNTO Y COMA (;)
				//OPERADORES DE COMBINACION == Es IGUALDAD 'Si son iguales...' ejem, (edad == 18).
				// SIGNO: != ESTO NIEGA LO QUE ESTÁ EJEMPLO 
				// > o >= Mayor o mayor igual, necesitan 2 operandos
				// < ó <= Menos o menor o igual
				// === IGUALDAD ESTRICTA EJEMPLO: Boolean 2 === "2" String, compara el valor.
				// == No es lo mismo que ===. 
				//Operadores Lógicos : && Y || ejemplo (edad==18  || sexo== 'M') 
				// Cuando es Y (&&): SOLO CUANDO LAS DOS SON VERDADERAS ES 'TRUE'. Sino, es falsa.
				// Cuando es O(||): Solo es 'FALSE' cuando los dos son 'False'. Sino, siempre es verdadera ('True');
				// POR CADA IF, SOLO HAY UN ELSE Y SE ESCRIBE FUERA DE {}.	


{
 var edad;
 document.getElementById('edad') ; 
 	if (edad==15)
 {alert ('Niña bonita');
 
 }

}//FIN DE LA FUNCIÓN